#include <stdio.h>
#include <stdlib.h>
/**
 * main - check the code
 * @argc: first par
 * @argv: second par
 * Return: Always 0
 */
int main(int argc, char *argv[])
	{
		printf("%s\n", argv[argc]);
		return (0);
	}
